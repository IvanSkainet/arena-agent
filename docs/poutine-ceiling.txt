9  # poutine finding ceiling. Ratchet: may fall, never rise.
   # 8x unverified-creator (all SHA-pinned), 1x untrusted_checkout_exec
   # in boe-contract (path-filtered, pip from a pinned lock).
   # 8 -> 9 ahead of the CodSpeed benchmark workflow (#291). The one new
   # finding is CodSpeedHQ/action, an eighth unverified creator, pinned to
   # the v5.2.1 commit SHA like the other seven; the untrusted_checkout_exec
   # is the same accepted one as before. Raised in its own change because the gate reads this
   # file from the BASE revision: if the bump travelled with the workflow,
   # the first master run after that merge would compare 9 findings against
   # a base that still said 8.
