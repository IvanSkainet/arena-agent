{
  "name": "wait-for-download",
  "title": "Kick off download, wait for file",
  "description": "Runs a shell command that starts a download in the background, then blocks until the expected file appears. Retries the whole cycle if the file never shows up in time. Illustrates v4.54.1 retry + wait_for.file.",
  "steps": [
    {
      "id": "start",
      "tool": "exec",
      "arguments": {
        "cmd": "touch /tmp/arena_demo_download.dat"
      },
      "retry": {
        "attempts": 3,
        "delay_seconds": 1.5,
        "backoff": 2.0
      },
      "wait_for": {
        "file": "/tmp/arena_demo_download.dat",
        "timeout_seconds": 30,
        "poll_seconds": 1
      }
    },
    {
      "id": "report",
      "return": "downloaded {{ steps.start.result.wait_for.size_bytes }} bytes in {{ steps.start.result.wait_for.waited_seconds }}s"
    }
  ]
}
