# Hash-locked tool for the mutation sweep job ONLY (Scorecard
# PinnedDependenciesID: every pip install in a workflow must resolve with
# hashes -- an unpinned `pip install mutmut==2.5.1` opened two alerts on
# the sweep workflow the day it landed).
#
# mutmut 2.5.1, NOT 3.x: 3.x copies the source tree into mutants/ and
# breaks this project's imports. Measured, not assumed.
# Regenerate: uv pip compile --universal --generate-hashes --strip-extras \
#   --output-file=requirements-mutation.lock requirements-mutation.in
mutmut==3.7.0
