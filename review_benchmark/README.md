# Reviewer Shadow Benchmark — NEVER MERGE

This directory exists only on the temporary T52 benchmark branch. It contains
synthetic, non-executed examples used to compare reviewer signal and false
positives. No fixture contains a real credential, performs a live exploit, or
belongs in a release artifact.

The pull request carrying this directory must be closed without merge after the
review evidence is collected.
