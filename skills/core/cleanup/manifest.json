{
  "name": "core/cleanup",
  "description": "Prune old backups, sessions, reports, completed tasks, rotated logs",
  "args": [
    {"name": "--days", "required": false},
    {"name": "--keep", "required": false},
    {"name": "--apply", "required": false},
    {"name": "--dry-run", "required": false}
  ],
  "timeout": 60,
  "mode": "edit"
}
